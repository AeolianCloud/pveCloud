# Phase 1：审计写入与脱敏收口

> 历史记录：本文件只记录 Phase 1 当时的审计写入和脱敏阶段背景。当前审计、权限和 API 契约以 `docs/server/`、`docs/server/api/` 和 `server/migrations/` 为准。

- [x] 更新审计写入契约，明确普通审计、高危双写、风险等级和失败策略
- [x] 建立集中审计写入服务
- [x] 建立统一脱敏规则
- [x] 建立高危动作矩阵
- [x] 补齐审计写入、敏感字段权限和高危双写测试

## Notes

本阶段完成后，后续管理功能不应再复制零散临时审计写入逻辑。

风险日志属于审计域，不单独拆成新的业务域。
