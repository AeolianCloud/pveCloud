# Phase 0：安全日志基座

- [x] 更新审计与高危日志相关 API 契约
- [x] 更新数据库设计与迁移，补入 `admin_risk_logs` 与相关权限边界
- [x] 补入后端查询模型、DTO、服务、Handler 和路由
- [x] 明确阶段边界与日志域职责
- [x] 完成这一阶段的基础实现与文档对齐

## Notes

本阶段主要建立“审计域存在且边界清晰”这一基础，而不是决定当前前端必须展示哪些页面。

后续如果前端页面范围收缩，不影响本阶段后端日志域能力仍然成立。
