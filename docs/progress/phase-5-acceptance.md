# Phase 5：验收和发布准备

- [x] 对齐接口文档、数据库设计、迁移、前端架构和实际路由菜单。
- [x] 补齐后端服务、权限、会话、审计和配置相关测试。
- [x] 运行 `go test ./...`。
- [x] 运行 `bun run build`。
- [x] 检查业务模块未出现在后台菜单和受保护路由中。

Notes:

本阶段完成后再提交基础后台阶段成果。

2026-04-26：后端测试和管理端构建已通过。最终提交前再做一次菜单和路由扫描。

2026-04-26：已扫描后台 fallback 菜单、前端受保护路由和后端菜单目录，未开放产品、订单、支付、实例、工单等业务模块。
