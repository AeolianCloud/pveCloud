# Phase 2：管理员和角色权限

- [x] 更新管理员、角色和权限接口契约的细节字段、错误码和高危动作说明。
- [x] 实现管理员后端服务、Handler、DTO 和路由。
- [x] 实现角色后端服务、Handler、DTO 和路由。
- [x] 实现权限码只读分组查询。
- [x] 实现管理端管理员列表、创建、编辑、启停、重置密码和角色分配页面。
- [x] 实现管理端角色列表、创建、编辑、启停和权限分配页面。
- [x] 补齐超级管理员保护、内置角色保护、权限边界和高危日志测试。

Notes:

权限码仍由迁移和种子维护，后台只读展示。

2026-04-26：管理员账号已接入 `/admin-api/admin-users`、创建、详情、更新和重置密码接口。管理端已增加 `/admin-users` 页面和菜单入口。角色分配先用角色 ID 输入承接，角色权限模块完成后替换成权限选择器。

2026-04-26：角色权限已接入 `/admin-api/admin-roles` 和 `/admin-api/admin-permissions`。管理端已增加 `/admin-roles` 页面，管理员账号表单已改为角色勾选。

2026-04-26：已补高危判定测试。当前超级管理员角色禁止禁用和改权限，当前登录账号禁止通过管理员更新接口禁用自己。
