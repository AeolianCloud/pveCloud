# Phase 1：审计写入和脱敏收口

- [x] 更新审计写入契约，明确普通审计、高危双写、风险等级和写入失败策略。
- [x] 实现集中审计写入服务，替代零散的临时写入逻辑。
- [x] 实现统一脱敏规则，覆盖密码、token、secret、验证码、敏感配置和常见密钥字段。
- [x] 建立高危动作矩阵，并接入登录限制、管理员、角色、会话和系统配置相关动作。
- [x] 补齐审计写入、敏感字段权限和高危双写测试。

Notes:

本阶段完成前，后续管理功能不要继续复制临时审计写入代码。

2026-04-26：已新增 `AdminAuditService.Record` 和 `AdminAuditService.RecordRisk`，登录、退出、刷新、登录限制和验证码限制已改为统一入口。读取旧日志时也会再次脱敏，避免历史明文字段被 `audit:sensitive_view` 直接放出。

2026-04-26：管理员、角色、会话吊销和系统配置更新已接入审计入口；高危动作走双写。已补审计脱敏和 RBAC 高危判定测试。
