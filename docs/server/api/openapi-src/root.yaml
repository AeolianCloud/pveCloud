# This file is an OpenAPI source fragment.
# Run `node ./scripts/generate-openapi.mjs` from the repository root to generate `docs/server/api/openapi.yaml`.
openapi: 3.1.0
info:
  title: pveCloud API
  version: 0.1.0
  description: pveCloud 用户端和管理端 API 规范。业务流程、事务和补偿策略以 docs/server/ 下对应设计文档为准。
servers:
  - url: http://localhost:8080
tags:
  - name: system
    description: 系统检查接口
  - name: admin-auth
    description: 管理端认证接口
  - name: admin-dashboard
    description: 管理端首页接口
paths:
{{paths}}
components:
  securitySchemes:
    AdminBearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  schemas:
{{schemas}}
