    ErrorResponse:
      allOf:
        - $ref: "#/components/schemas/ResponseEnvelope"
        - type: object
          properties:
            code:
              type: integer
              example: 50001
            message:
              type: string
              example: 系统内部错误
            data:
              nullable: true
