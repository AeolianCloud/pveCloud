  /api/ping:
    get:
      tags:
        - system
      summary: 用户端 API 入口检查
      operationId: getApiPing
      responses:
        "200":
          description: 用户端 API 可用
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PingResponse"
