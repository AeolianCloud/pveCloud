# API 约定

API 响应、错误码、鉴权和幂等约定已经迁移到：

```text
.codex/skills/pvecloud-document-first/references/backend.md
```

接口最终契约仍是 OpenAPI：

```text
docs/server/api/openapi.yaml
```

保留本文件仅用于兼容旧链接。后续维护请同步技能 reference 和 OpenAPI。
