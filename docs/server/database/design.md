# 数据库设计

数据库设计规则、表分组、索引、约束和事务边界已经迁移到：

```text
.codex/skills/pvecloud-document-first/references/database.md
```

可执行迁移契约仍保留在：

```text
server/migrations/
```

保留本文件仅用于兼容旧链接。后续表结构变更请同步技能 reference 和迁移 SQL。
