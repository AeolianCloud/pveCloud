// Package middleware contains web-boundary middleware for /api/* handlers.
//
// The current repository stage only exposes the minimal web ping endpoint, so
// this package is intentionally light until real web auth or request-chain
// behavior is introduced.
package middleware
